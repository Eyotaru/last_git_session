const factorial =(number) => {
    if (number <= 1) {
        return 1;
    }
    return number*factorial(number-1)
};const factorial =(number) => {
    if (number <= 1) {
        return 1;
    }
    return number*factorial(number-1)
};
console.log(factorial(10))
const power = (a, b) => a ** b;
const max = (a, b) => Math.max(a, b);git 